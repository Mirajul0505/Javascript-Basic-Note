
`                                                              **Java Script Basic Note:**

      👉 দুটো result বাইরে দরকার হলে object return করো আর sequence দরকার হলে array return করো       
          শুধু debug করতে চাইলে console.log করো
      👉 ফাংশনের আউটপুট forEach-এ ব্যবহার করতে হলে, callback-এর ভিতরে ফাংশন কল করে      
         তার  return ভ্যালু দিয়ে কাজ করতে হবে
      👉 যখন জাভাস্ক্রিপ্ট এ +  দ্বারা কোনো object (array)  তে স্যংখা  যোগ করা হবে তখন জাভাস্ক্রিপ্ট  
         ওই array টিকে স্বয়ংক্রিয় ভাবে  স্ট্রিং এ রুপান্তর করে।
      👉 শুধু লুপ চালানোর জন্য forEachব্যাবহার করো। forEach কোনো কিছু return নেয় না বা দেয় না ।  
         কিন্তু তুমি ফাংশনের আউটপুট নিয়ে forEach–এর ভেতরে কাজ করতে পারো । 
      👉 ফাংশনের আউটপুট নিয়ে নতুন array তৈরি করতে map ব্যাবহার করো।  map নিজেই ফাংশনের return ভ্যালু নিয়ে array বানায়। 
         element সবসময় array-এর আসল মানটাই হবে। return করা ভ্যালু দিয়ে element পরিবর্তন হয় না।
      👉 শর্ত অনুযায়ী বাছাই করতে filter ব্যাবহার করো।
      
      👉**প্রোগ্রামিংয়ের ভাষায়, যখন কোনো ভেরিয়েবলের মান হিসেবে একটি ফাংশনকে রাখা হয়, তখন তাকে সাধারণত
          First-class Function (ফার্স্ট-ক্লাস ফাংশন) বলা হয়।**
        ​সহজ কথায়, যদি কোনো ল্যাঙ্গুয়েজ ফাংশনকে একটি ভেরিয়েবল হিসেবে ব্যবহার করার অনুমতি দেয় (যেমন: ভেরিয়েবলে অ্যাসাইন করা, 
        অন্য ফাংশনের আর্গুমেন্ট হিসেবে পাঠানো বা ফাংশন থেকে রিটার্ন করা), তবে সেই ফাংশনগুলোকে ফার্স্ট-ক্লাস সিটিজেন বা ফাংশন বলা হয়।
        ​এর সাথে সংশ্লিষ্ট আরও কিছু গুরুত্বপূর্ণ ধারণা নিচে দেওয়া হলো:
        ​১. ফাংশন এক্সপ্রেশন (Function Expression)
        ​যখন আপনি একটি ফাংশন তৈরি করে সরাসরি একটি ভেরিয়েবলে জমা রাখেন, তখন সেই প্রক্রিয়াটিকে বলা হয় Function Expression।
        
        const sayHello = function() {
          console.log("হ্যালো!");
        };
        এখানে sayHello একটি ভেরিয়েবল যার মান হলো একটি ফাংশন।
        
        **২. হায়ার-অর্ডার ফাংশন (Higher-Order Function):**
        ​যখন কোনো ফাংশন অন্য একটি ফাংশনকে আর্গুমেন্ট হিসেবে গ্রহণ করে অথবা অন্য একটি ফাংশনকে রিটার্ন করে, তখন তাকে
        Higher-Order Function বলে।
       ** ​৩. কলব্যাক ফাংশন (Callback Function)**
        ​যখন একটি ফাংশনকে অন্য একটি ফাংশনের ভেতর প্যারামিটার বা মান হিসেবে পাঠানো হয় এবং পরে সেটিকে কল করা হয়, তখন ওই ফাংশনটিকে 
        বলা হয় Callback Function।
      👉**হায়ার অর্ডার ফাংশন (Higher-Order Function) হলো এমন একটি ফাংশন যা অন্য কোনো ফাংশনকে ইনপুট (Argument) হিসেবে গ্রহণ করে
       অথবা একটি নতুন ফাংশনকে  আউটপুট (Return) হিসেবে দেয়।**
        ​সাধারণ ফাংশন শুধু ডেটা (যেমন: সংখ্যা বা স্ট্রিং) নিয়ে কাজ করে, কিন্তু হায়ার অর্ডার ফাংশন ফাংশন নিয়ে কাজ করে।
        ​১. কেন একে হায়ার অর্ডার ফাংশন বলা হয়?
        ​প্রোগ্রামিংয়ে ফাংশন যখন সাধারণ ডেটার মতো আচরণ করে (অর্থাৎ ভেরিয়েবলে রাখা যায়, আর্গুমেন্ট হিসেবে পাঠানো যায়), তখন তাকে
        First-class citizen বলা হয়। আর এই           সুযোগ ব্যবহার করে যে ফাংশনগুলো অন্য ফাংশনকে নিয়ন্ত্রণ করে, তারাই হায়ার অর্ডার ফাংশন।
        ​২. এটি কীভাবে কাজ করে? (দুইটি উপায়)
        ​ক. ফাংশনকে আর্গুমেন্ট হিসেবে গ্রহণ করা:
        ​সবচেয়ে পরিচিত উদাহরণ হলো map(), filter(), forEach()।
        ​উদাহরণ (JavaScript):
        
        function greet(name) {
            return "হ্যালো " + name;
        }
        
        // এখানে 'callUser' একটি হায়ার অর্ডার ফাংশন
        function callUser(callback, userName) {
            return callback(userName); 
        }
        
        console.log(callUser(greet, "রাহাত")); // আউটপুট: হ্যালো রাহাত
        
        খ. ফাংশনকে রিটার্ন করা:
        ​যখন একটি ফাংশন নিজে শেষ হয়ে যাওয়ার সময় আরেকটি ফাংশন তৈরি করে দিয়ে যায়।
        ​উদাহরণ:
        
        function multiplier(factor) {
            return function(num) {
                return num * factor;
            };
        }
        
        const double = multiplier(2);
        console.log(double(5)); // আউটপুট: 10
        
       ** ৩. বহুল ব্যবহৃত কিছু বিল্ট-ইন হায়ার অর্ডার ফাংশন**
        map() =>একটি অ্যারের প্রতিটি উপাদানে কোনো কাজ করে নতুন অ্যারে তৈরি করে।
        filter() =>শর্ত সাপেক্ষে অ্যারে থেকে নির্দিষ্ট কিছু উপাদান বাছাই করে।
        reduce() =>পুরো অ্যারের সব উপাদানকে প্রসেস করে একটি মাত্র মান (Result) দেয়।
        sort() =>একটি নির্দিষ্ট লজিক অনুযায়ী অ্যারে সাজায়।
        
        ৪. হায়ার অর্ডার ফাংশন ব্যবহারের সুবিধা
        ​কোড রিইউজেবিলিটি (Reuseability): একই লজিক বারবার না লিখে একটি ফাংশন বানিয়ে তা অন্য জায়গায় পাস করা যায়।
        ​ক্লিন কোড: অনেক বড় লুপ বা কন্ডিশন না লিখে ছোট ছোট কলব্যাক ফাংশন দিয়ে জটিল কাজ করা যায়।
        ​ডিক্লারেটিভ প্রোগ্রামিং: এটি "কিভাবে করতে হবে" তার চেয়ে "কী করতে হবে" তার ওপর বেশি গুরুত্ব দেয়।
        ​৫. কলব্যাক (Callback) বনাম হায়ার অর্ডার ফাংশন ​অনেকে এই দুইটির মধ্যে গুলিয়ে ফেলেন।
        **মনে রাখবেন:**
          ** * ​যে ফাংশনটি আপনি পাচ্ছেন বা গ্রহণ করছেন, সেটি হলো হায়ার অর্ডার ফাংশন।**
         **  * ​যে ফাংশনটিকে আপনি পাঠাচ্ছেন (Input দিচ্ছেন), সেটি হলো কলব্যাক ফাংশন।**
      **নোট: ** যে ফাংশন টিকে আমরা অন্য কোন ফাংশন এর প্যারামিটার হিসেবে দিবো সেটি হবে কলব্যাক ফাংশন।   আর যে ফাংশনের মাধ্যে দেওয়া হবে  
       সেটি হবে হায়ার অর্ডার ফাংশন।
     **   হায়ার অর্ডার ফাংশন কল বা রিটার্ন করার প্রয়োজনিয়তা :**
        ১.যেকোনো ফাংশনের মতো হায়ার অর্ডার ফাংশনকেও কল করতে হয়, নাহলে তার ভেতরের লজিক বা লুপ শুরুই হবে না।
         **কল না করলে কি হবে?
              ​কল না করলে: ফাংশনটি কেবল মেমোরিতে জমা থাকবে।
              ​কল করলে: যখনই কল করা হবে তখনই সে তার প্যারামিটার অনুযায়ী প্রসেস শুরু করবে।
        ২. হায়ার অর্ডার ফাংশন থেকে 'রিটার্ন' (Return) করার প্রয়োজনীয়তা
        ​হায়ার অর্ডার ফাংশনে রিটার্ন করার বিষয়টি কেন দরকার, তা দুটি প্রেক্ষাপটে দেখা যায়:
        ​ক. যখন আপনি নতুন ডেটা পেতে চান (যেমন: map বা filter)
        ​যদি আপনার ফাংশনটি কোনো অ্যারে প্রসেস করে নতুন কিছু তৈরি করে, তবে সেটি রিটার্ন না করলে আপনি বাইরে থেকে সেই ফলাফলটি পাবেন না।
        
        খ. যখন আপনি একটি ফাংশন তৈরি করতে চান
        ​কিছু হায়ার অর্ডার ফাংশন ডেটা রিটার্ন না করে একটি নতুন ফাংশন রিটার্ন করে। একে বলা হয় Function Factory।
        function multiplier(factor) {
            return function(num) { // এখানে একটি ফাংশন রিটার্ন করা হচ্ছে
                return num * factor;
            };
        }
        
        const double = multiplier(2); // 'double' এখন একটি ফাংশন
        console.log(double(10)); // ২০
        
        ৩. কখন রিটার্ন করার প্রয়োজন নেই?
        ​যদি হায়ার অর্ডার ফাংশনের কাজ শুধু কিছু প্রদর্শন করা বা প্রিন্ট করা হয় (যেমন: forEach), তবে কিছু রিটার্ন করার প্রয়োজন নেই।
        
      👉ফাংশনের ভেতরে আরেকটি ফাংশন তৈরি করতে হলে let fx = () => {} অথবা সরাসরি function fx() {} লিখতে হয়। fx: () => {} এর ব্যবহার: এটি অবজেক্টের ভেতর              মেথড লেখার নিয়ম। 
      
      👉** x++,  x +=1  এবং ++x   এর পার্থক্য :**

         **১.x ++ **:
          x=x+1;  এই কন্ডিশন শুধুমাএ ম্যাথমেটিক্যাল ভ্যালু এড করার জন্য ব্যাবহার করা হয়। শুধু মাএ নম্বর ডাটা টাইপ এর পূর্ববর্তী ভ্যালুর সাথে ১ যোগ করে।
        **২.x +=1 :**
         x=x+1;  স্ট্রিং এর সাথে স্ট্রিং কনকেটেনেশনের মাধ্যমে  ভ্যালু যোগ করা হয় । পূর্ববর্তী ভ্যালুর সাথে যে কোনো সংখ্যা  যোগ করা যায়।
        
        ** ৩.  ++x :**
        প্রোগ্রামিংয়ের ভাষায় ++x কে বলা হয় Prefix Increment (প্রি-ফিক্স ইনক্রিমেন্ট)। সহজ কথায় এর মানে হলো— আগে ১ যোগ করো, তারপর মানটি ব্যবহার করো।
        ​এটি বুঝতে হলে আপনাকে দুটি ধাপ খেয়াল করতে হবে:
        ১. এটি ভেরিয়েবল x এর মান ১ বাড়িয়ে দেয়।
        ২. বাড়ানোর পর যে নতুন মানটি পাওয়া গেল, সেটিই প্রসেস বা এক্সপ্রেশনে ব্যবহার হয়।
        
      👉
      👉 **স্ট্রিং মেথড :**
                  1. length: স্ট্রিং এর দৈর্ঘ্য বের করে।
                  2.toUpperCase ():স্ট্রিং কে বড় হাতের অক্ষরে রুপান্তর করে।
                  3.toLowerCase(): স্ট্রিং কে ছোট হাতের অক্ষরে রুপান্তর করে।
                  4.trim(): শুরু ও শেষের ফাকা জায়গা মুছে দেয়।
                  5.split(): স্ট্রিং কে ভাগ করে নতুন এ্যারে বানায়। 
                  6.includes(): চেক করে স্ট্রিং আছে কিনা। 
                  7.startWith(): চেক করে স্ট্রিং কি দিয়ে শুরু হয়েছে।
                  8.endWith(): চেক করে স্ট্রিং কি দিয়ে শেষ হয়েছে।
                  9.charAt(index): নির্দিষ্ট অবস্থানে অক্ষর দেয়।
                  10.indexOf(subStr): সাব-স্ট্রিং এর প্রথম ইনডেক্স দেয়।
                  11.replace(): স্ট্রিং এর নির্দিষ্ট অংশ অন্য কোন স্ট্রিং দ্বারা পরিবর্তন করে। 
                  12.repeat(n): স্ট্রিং  n বার রিপিট করে।
                  
                 ** নোট: স্ট্রিং মেথড সব সময় স্ট্রিং রিটার্ন করে।**
                 
      👉**অ্যারে মেথড:**
                  1. push(): এ্যারের  শেষে নতুন এলিমেন্ট যোগ করে। 
                  2. pop(): এ্যারের শেষ এলিমেন্ট কে মুছে বা ডিলিট করে। 
                  3. shift(): এ্যারের প্রথম এলিমেন্ট কে মুছে বা ডিলিট করে। 
                  4. unshift(): এ্যারের শুরুতে এলিমেন্ট যোগ করে। 
                  5. concat(): দুই বা ততোধিক এ্যারে কে একত্রে মিশায়। 
                  6. Join(separator): এ্যারের সব এলিমেন্ট স্ট্রিং এ মিলায়। 
                  7. slice(start,end ):  একটি এ্যারের নির্দিষ্ট অংশ কেটে নতুন এ্যারে বানায়। 
                  8. Splice(start,delet,count,newItem): এ্যারে থেকে এলিমেন্ট মুছে বা যোগ করে। 
                  9. forEach(): প্রতিটি এলিমেন্ট কে নিয়ে কাজ করে। রিটার্ন নেই। 
                  10. map(): প্রতিটি এলিমেন্ট কে নিয়ে কাজ করে নতুন এ্যারে তৈরি করে।
                  11. filter(): শর্তানুযায়ী নতুন এ্যারে বানায়। 
                  12. reduce(): এ্যারের সব এলিমেন্ট কে এক মানে কম্বাইন্ড করে।
                  13. find(): শর্তানুযায়ী  মেলানো প্রথম এলিমেন্ট  দেয়।
                  14. findIndex(): শর্তানুযায়ী প্রথম মেলানো এলিমেন্টের ইনডেক্স  দেয়।
                  15. some(): কোনো এক এলিমেন্ট শর্ত পুরন করে কিনা চেক করে। 
                  16. every(): সব এলিমেন্ট শর্ত পুরন করে কিনা চেক করে। 
                  17. sort(): এ্যারের এলিমেন্ট সাজায়। 
                  18. reverse(): এ্যারের এলিমেন্ট উল্টে দেয়। 
                  19. include (): এলিমেন্ট আছে কিনা চেক করে ।

      👉**স্ট্রিং থেকে অবজেক্ট এ রুপান্তর :**

                  ১.যদি স্ট্রিংটি JSON ফরম্যাটে থাকে, তাহলে JSON.parse() ব্যবহার করা হয়।
                  ২. অবজেক্ট এর মতো স্ট্রিং হলে eval() ব্যবহার করা ঝুঁকিপূর্ণ, কারণ এতে ক্ষতিকর কোড চলতে পারে।
                  ৩.URL Query String হলে URLSearchParams() ব্যাবহার করা। 
                  যেমন :
                  
                  const str = "name=Rahim&age=20&city=Dhaka";
                  
                  const obj = Object.fromEntries(new URLSearchParams(str));
                  
                  console.log(obj);
                  
                  ৪. কাস্টম স্ট্রিং ফরমেটে হলে split () ব্যাবহার করতে হবে। 
                  
                  যেমন :
                  
                  const str = "name:Rahim,age:20,city:Dhaka";
                  
                  const obj = {};
                  
                  str.split(",").forEach(item => {
                    const [key, value] = item.split(":");
                    obj[key] = value;
                  });
                  
                  console.log(obj);

      👉**নোট:**
                  ১. string-এর উপর map() চলে না
                  map() শুধু array এর জন্য।
                  ২.for...of শুধু iterable (Array, String, Map, Set) এর জন্য।
                  ৩.🧠 কখন কোনটা ব্যবহার করবেন?
                     Array / String → for...of
                     নির্দিষ্ট শর্তে loop → while
                     Index দরকার হলে → for
                  ৪.JavaScript এ:
                      undefined → false
                     সংখ্যা 1, 2, 3... → true
                  👉 obj[ch] মানে হলো:object-এর সেই property যার নাম হলো ch    এর মান/value;
                  ৫.  [] → dynamic key
                     .   → fixed key


      👉
      👉
      👉
      👉
      
      👉
      👉
      👉
      👉
      
      👉
      👉
      👉
      👉
      
      👉
      👉
      👉
      👉
      
      👉
      👉
      👉
      👉
      
      👉
      👉
      👉
      👉
      
      👉
      👉
      👉
      👉
      
      👉
      👉
      👉
      👉
      
      👉
      👉
      👉
      👉
      
      👉
      👉
      👉
      👉
      
      👉
      👉
      👉
      👉
      
      👉
      👉
      👉
      👉
      
      👉
      👉
      👉
      👉
      
      👉
      👉
      👉
      👉
      
      👉
      👉
      👉
      👉
      
      👉
      👉
      👉
      👉

      
      
